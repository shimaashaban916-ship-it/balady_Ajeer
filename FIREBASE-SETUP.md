# 🔥 دليل إعداد Firebase

## 📌 الخطوات (5 دقائق):

### 1️⃣ إنشاء مشروع Firebase

1. اذهب إلى: https://console.firebase.google.com
2. اضغط **"Add project"** (إضافة مشروع)
3. اكتب اسم المشروع: `ajeer-contracts`
4. اضغط **Continue** → **Continue** → **Create project**

---

### 2️⃣ إضافة تطبيق Web

1. في لوحة التحكم، اضغط على أيقونة **</>** (Web)
2. اكتب اسم التطبيق: `Ajeer Frontend`
3. **لا تفعل** Firebase Hosting (مش محتاجينه)
4. اضغط **Register app**

---

### 3️⃣ نسخ الإعدادات

ستظهر لك شاشة فيها كود JavaScript. **انسخ الجزء الخاص بـ `firebaseConfig`**:

```javascript
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "ajeer-contracts.firebaseapp.com",
  projectId: "ajeer-contracts",
  storageBucket: "ajeer-contracts.appspot.com",
  messagingSenderId: "123...",
  appId: "1:123..."
};
```

---

### 4️⃣ لصق الإعدادات في المشروع

1. افتح ملف: `js/firebase-config.js`
2. **استبدل** السطور من 8 إلى 14 بالكود اللي نسخته

**قبل:**
```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",  // ← استبدل هذا
    // ...
};
```

**بعد:**
```javascript
const firebaseConfig = {
    apiKey: "AIzaSyC...",  // ← القيم الحقيقية من Firebase
    authDomain: "ajeer-contracts.firebaseapp.com",
    projectId: "ajeer-contracts",
    // ...
};
```

---

### 5️⃣ تفعيل Firestore Database

1. في القائمة الجانبية، اضغط **Build** → **Firestore Database**
2. اضغط **Create database**
3. اختر **Start in test mode** ← **Next**
4. اختر المنطقة: `europe-west2` (لندن) أو أي منطقة قريبة
5. اضغط **Enable**

---

### 6️⃣ إعداد قواعد الأمان (Rules)

في صفحة Firestore، اضغط على تبويب **Rules** والصق هذا الكود:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /contracts/{document=**} {
      allow read, write: if true;
    }
  }
}
```

ثم اضغط **Publish**

⚠️ **ملاحظة:** هذه القواعد تسمح بالقراءة والكتابة للجميع (للتجربة فقط).
في الإنتاج، لازم تضيف نظام مصادقة.

---

## ✅ اختبار الإعداد

1. افتح `index.html` في المتصفح
2. افتح **Developer Console** (F12)
3. لو شفت رسالة: `🔥 Firebase Mode: Active` → **تمام!**
4. لو شفت: `💾 LocalStorage Mode: Active` → راجع الخطوات

---

## 🎯 الآن يمكنك:

✅ إضافة عقود → **تُحفظ في Firebase**
✅ تعديل عقود → **يتحدث في Firebase**
✅ حذف عقود → **يُحذف من Firebase**
✅ **البيانات مشتركة** بين كل الأجهزة والمستخدمين

---

## 🚀 النشر على Vercel

بعد إعداد Firebase:

```bash
git add .
git commit -m "Added Firebase integration"
git push
```

وVercel هيشتغل عادي! 🎉

---

## ❓ مشاكل شائعة

### المشكلة: `Firebase not configured`
**الحل:** تأكد إنك نسخت `firebaseConfig` صح في `js/firebase-config.js`

### المشكلة: `Permission denied`
**الحل:** راجع قواعد الأمان (Rules) في Firestore

### المشكلة: البيانات ما تظهر
**الحل:** افتح Console واشوف أي أخطاء

---

## 📊 حدود المجانية في Firebase

| العملية | الحد المجاني |
|---------|--------------|
| قراءة | 50,000 يومياً |
| كتابة | 20,000 يومياً |
| حذف | 20,000 يومياً |
| تخزين | 1 GB |

**يعني:** كافي لآلاف المستخدمين! 🚀
