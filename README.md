# 🏢 نظام عقود أجير - Ajeer Contracts System

نظام إدارة عقود العمالة المؤقتة - Frontend فقط مع Firebase

## ✨ المميزات

- ✅ **Frontend فقط** - بدون Backend معقد
- ✅ **Firebase Firestore** - قاعدة بيانات سحابية مجانية
- ✅ **LocalStorage Fallback** - يشتغل بدون Firebase
- ✅ **QR Code** - باركود لكل عقد
- ✅ **Scanner** - مسح الباركود من الهاتف
- ✅ **PDF Export** - تصدير العقود كـ PDF
- ✅ **جاهز للنشر** - Vercel/Netlify/Firebase Hosting
- ✅ **بيانات مشتركة** - كل المستخدمين يشوفوا نفس البيانات

## 📁 الملفات

```
AjeerFrontend/
├── index.html          # الصفحة الرئيسية (قائمة العقود)
├── form.html           # نموذج إضافة/تعديل عقد
├── contract.html       # عرض العقد وطباعته
├── verify.html         # التحقق من العقد (للباركود)
├── scanner.html        # مسح الباركود
├── css/
│   └── style.css      # التنسيقات
├── js/
│   ├── app.js         # المنطق الأساسي
│   └── sample-data.js # بيانات تجريبية
└── img/               # الشعارات والصور
```

## 🚀 التشغيل المحلي

### بدون Firebase (للتجربة السريعة):
1. افتح `index.html` في المتصفح مباشرة
2. سيعمل بنظام LocalStorage (البيانات محلية فقط)

### مع Firebase (البيانات المشتركة):
1. اتبع دليل **[FIREBASE-SETUP.md](FIREBASE-SETUP.md)** (5 دقائق)
2. افتح `index.html` في المتصفح
3. البيانات الآن محفوظة في السحابة! ☁️

## 📤 النشر على Vercel

### الطريقة الأولى: من GitHub

1. **ارفع على GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin YOUR_GITHUB_URL
   git push -u origin main
   ```

2. **اربط بـ Vercel:**
   - اذهب إلى [vercel.com](https://vercel.com)
   - اضغط "Import Project"
   - اختر الريبو من GitHub
   - اضغط Deploy

### الطريقة الثانية: Vercel CLI

```bash
npm i -g vercel
vercel
```

## 💾 إدارة البيانات

### تحميل بيانات تجريبية:
افتح Console في المتصفح واكتب:
```javascript
loadSampleData()
```

### معرفة الوضع الحالي:
- لو شفت `🔥 Firebase Mode: Active` → البيانات في السحابة
- لو شفت `💾 LocalStorage Mode: Active` → البيانات محلية فقط

### حذف البيانات المحلية:
```javascript
localStorage.clear()
```

**ملاحظة:** لحذف بيانات Firebase، استخدم لوحة تحكم Firebase Console.

## 📱 استخدام Scanner

1. افتح `scanner.html` من هاتفك
2. امسح الباركود من العقد
3. سيتم فتح صفحة التحقق تلقائياً

## 🔧 التخصيص

- **تغيير الشعارات:** ضع الصور في مجلد `img/`
- **تعديل الألوان:** عدّل ملف `css/style.css`
- **إضافة حقول:** عدّل `form.html` و `app.js`

## 📝 ملاحظات

- البيانات محفوظة في LocalStorage
- الباركود يعمل بدون إنترنت (إذا فُتح محلياً)
- للنشر على Vercel: البيانات ستكون منفصلة لكل مستخدم

## 🎯 الاستخدام

1. **إضافة عقد:** اضغط "عقد جديد"
2. **عرض عقد:** اضغط على أيقونة العين
3. **طباعة PDF:** من صفحة العقد اضغط "تحميل PDF"
4. **مسح باركود:** افتح Scanner من هاتف آخر

---

تم التطوير بواسطة Claude Code 🤖
